import { LoadingWrapper, Title } from "./style"

const Loading = () => {
    return (
        <LoadingWrapper>
            <Title>Contact List</Title>
        </LoadingWrapper>
    )
}

export default Loading